node_modules
backend/node_modules
frontend/node_modules
frontend/dist
backend/dist
backend/data
package-lock.json
test_subscription_url
.DS_Store
coverage/
*.log

# Local scratch files. Keep real subscription URLs and ad-hoc test inputs here.
tmp/*
!tmp/.gitkeep

# Local deployment configuration. Commit only *.example files.
.env
.env.*
!.env.example
deploy/deploy.env
deploy/nginx-proxyparser.conf

# Local deployment artifacts. Build images locally and upload them; do not commit image archives.
dist/
*.tar
*.tar.gz
